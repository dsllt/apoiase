import styled from 'styled-components'

export const Container = styled.button`
  background: none;
  border: none;

  font-size: 16px;
  font-weight: bold;

  display: flex;
  gap: 20px;
  align-items: center;

  svg {
    height: 30px;
    width: 30px;
  }
`
